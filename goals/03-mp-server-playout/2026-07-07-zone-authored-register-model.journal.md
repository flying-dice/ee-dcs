# Journal: MP-server playout — 2026-07-07 (zone-authored register-not-spawn model)

**Goal:** goals/03-mp-server-playout/GOAL.md
**Session focus:** Pivot the whole theatre-authoring model to ME-zone-authored + register-not-spawn, plus map indicators, warnings, and the v0.1.0 release. Long session; many small iterations driven live by the user.

## Log

### Zone-authored theatre (colour = side, name = type)
- `zones.lua` (NEW earlier, extended heavily): reads ME trigger zones (`env.mission.triggers.zones`), circle + quad. Keysite zone = `<type>_<label>` name (leading LETTERS → type, so `airbase_batumi`/`airbase-1`/`farp 2` all resolve; regex `^%s*(%a+)`), **zone COLOUR → side** (`side_of_color`: blue-dominant BLUE, red-dominant RED; `z.color` = {r,g,b} array). Exports `M.keysites()`, `M.has_keysite_zones()`, `M.exists/contains/statics_in_zone/scenery_in_zone`.
- Types: airbase, farp, factory, refinery, port, radar, depot, fuel, power, command.
- `keysite.lua init_base_state`: NEW zone-authored branch (when `has_keysite_zones()`) — airbase zones bind to nearest DCS AIRDROME; farp zones → FARP-<label> heli bases; ownership from colour (NO x-median split). Falls back to BLUE/RED-box scoping → THEATRE box → auto densest-cluster.
- `farps.lua`: zone mode skips auto classification; spawns FARP statics only for farp bases (later: still user-places FARP statics).
- **README.md**: added full "Authoring a mission — the zone-based theatre" guide (convention table, 10 keysite types, balance table per side [1-2 airbase, 3-6 farp, ~12-20 total, farp:airbase 3:1], step-by-step). Naming standard is `<type>_<label>` e.g. `airbase_batumi`.
- Verified live: 4 airbases + 5 FARPs + 18 keysites from the user's 27-zone mission; labels render.

### v0.1.0 RELEASE cut
- `game_loop.version` 0.3.0→**0.1.0**. Added `VERSION` (0.1.0) + `CHANGELOG.md` (full feature list + known limitations). NOT a git repo — release = versioned + clean build + changelog. Journaled in the prior entry too.

### REGISTER-not-spawn (binding user directive — see memory feedback_register_not_spawn.md)
- User: **"YOU DO NOT PLACE THINGS, YOU REGISTER PLACED ITEMS WITHIN THE TRIGGER ZONE."** The campaign must spawn/place NOTHING; the user places all statics + zones; the campaign registers placed objects inside each keysite circle as that keysite's assets.
- Removed all spawning from the zone path: `installations.lua` (registers `zones.statics_in_zone` as `rec.assets`, no addStaticObject), `farps.lua` (no FARP spawn in zone mode), `base_defenses.lua` (returns early / no AD garrison + no regarrison in zone mode). Auto path (no zones) still spawns as fallback.
- Keysite health = alive assets / total; `M.damage` destroys `ceil(amount*total)` still-alive statics; at <MINIMUM_EFFICIENCY neutralised/capturable (record KEPT). Static-death world event handler (`S_EVENT_DEAD/KILL` → `register_static_death`), re-injection safe via `_G.__dmt_static_death_handler`.
- Airbase decision (user Q&A): airbase assets = DCS **warehouse** (read via `Airbase:getWarehouse():getInventory()` → aircraft/weapon/liquids; fuel 1e6 = unlimited default) stored on `S.base_warehouse` **+** placed statics. Draining to zero = **neutralised/capturable** (not removed). NOTE: user explicitly chose EECH economy, NOT warehouse-driven OOB — warehouse is read/registered but does not drive sortie generation.
- Verified: user's 22 placed statics registered (command-1=7, port=15), 0 spawned; `Command Post [BLUE] 100% (7/7)` on map.

### Empty-zone "pending" handling
- Zones with 0 assets = `pending` (excluded from strike/economy/production/win, not "neutralised"); shown as "— empty (place statics)". `low_targets` = 1..2 assets. Guards added to `production_rates`/`strike_targets`/`strike_targets_ranked` (`not rec.pending`).

### No/low-targets WARNING (user request)
- `installations.lua init_from_zones`: `LOW_TARGET_THRESHOLD=2`; collects pending+low keysites → boot-log `WARNING: N keysite(s) have no/low targets ...` AND **in-game `trigger.action.outText`** (30s) AND **`⚠ LOW TARGETS`** prefix on the F10 label (map_overlay). Verified: 16-17 keysites flagged with counts.

### Scenery-as-drainable-assets (airfield built-in fuel/warehouse)
- Scenery IS enumerable (`world.searchObjects(Object.Category.SCENERY, sphere, handler)`), destroyable (`getLife`), stable numeric name id, but **NOT re-acquirable by name** → must HOLD the handle and poll `getLife`. Verified held handles stay pollable across calls.
- `zones.scenery_in_zone(zone, type_set)` + `M.RESOURCE_SCENERY` allowlist + `is_resource_scenery` pattern. `rec.scenery = {{id,type,handle,dead}}`; folds into total/alive/health. `M.poll_scenery` every **25s** (game_loop schedule), marks dead when life<=1. `M.damage` also destroys scenery.
- **Russian type names** were the gotcha: DCS Caucasus fuel = `TOPLIVO-BAK_NEW`, warehouse = `SKLADIK` (English `FUEL/WARE` patterns missed them). `SKLAD_NEW` is generic civilian (67 across Batumi town) — EXCLUDED. Final allowlist: `AIRBASE_BARRELS_01, PAR_GSM, TOPLIVO-BAK_NEW, SKLADIK` + `TOPLIVO` pattern.
- Batumi = 5 (3 fuel TOPLIVO-BAK + barrels + SKLADIK) = matches user's "3 fuel + 2 warehouses". **Kutaisi = 76, Gudauta = 58, Sochi = 75** — I initially thought FLOOD and disabled the scan, but USER CONFIRMED these are CORRECT (Kutaisi genuinely ~62 warehouses + ~18 fuel; big airfield). RE-ENABLED the scan (keysite.lua + installations.lua). ~258 per-destructible marks total, sim advancing fine (t=300, no freeze).

### Per-destructible map indicators (user request)
- `map_overlay.lua update_asset_indicators`: one small **`▪`** mark (id range 70000) per registered static + scenery, placed once, removed when destroyed (thins out as bombed). `reset.lua` clears 70000-70400.
- User asked why marks say "mission data": that's DCS's fixed **author** attribution for ALL scripted `markToAll` marks (unchangeable). Fix = descriptive TEXT: `▪ <keysite label> (target)` / `▪ Airfield assets Batumi (TOPLIVO-BAK_NEW)`.

### Air-defence-disappears (investigated, NOT changed)
- Root cause: `reset.nuke` step 2 destroys EVERY non-player group on re-inject (fresh OOB without restart), incl. author-placed AD (SAMs/AAA/EWR are groups; statics survive since nuke only hits groups). Drafted a fix (capture author-placed groups once in `_G.__dmt_preplaced`, skip in nuke) but **user said "actually no that's fine" → REVERTED**. reset.lua is back to original. AD only vanishes on re-inject, not on fresh mission start.

## Persistent map mark ID ranges (map_overlay)
- 50000-50200 installation labels · 60000-60060 base labels · 70000-70400 per-destructible ▪ · 40000 frontline · 30000 columns · 20000 task arrows (disabled). Multi-reinject in one session re-triggers stale-scheduler CLOBBERING (old-gen overlay timers wipe 50k/60k) — fresh restart + SINGLE inject is the clean-view workaround (unfixed backlog).

## Current state
- Build 29 modules 0 warnings, check 31 files 0 findings, dist current. Working tree clean (reset.lua reverted).
- Full authored pipeline live & verified: colour zones → sides/bases/keysites; register placed statics + resource scenery (bounded by zone) as drainable assets; per-destructible ▪ + keysite (alive/total) + ⚠ low-target + in-game warning; warehouse read; EECH economy.

### FARPs don't launch helis (root-caused; launch FIXED, persistence PENDING)
- Symptom: `task_board: cas/bai vs field EXPIRED (no suitable idle group)`, 0 live helis, 13 planes fine.
- Root cause: zone-authored farp bases are named `FARP-farp-N` at the zone centre but there is **NO physical HELIPAD airbase** of that name (`Airbase.getByName("FARP-farp-1")`==nil; `world.getAirbases()` has ZERO HELIPADs — register-not-spawn stopped FARP static spawns and none are bound). `heli_war.build_attack_heli` did `if not home_ab then return nil end` → every CAS/BAI heli spawn failed silently → board couldn't assign.
- User insight: helis CAN launch from fixed-wing airbases (airbase ledger has heli stock, e.g. `idle … H6`), so a base without its own pad should still launch from its coordinates.
- FIX APPLIED (`heli_war.build_attack_heli`): when `Airbase.getByName(base_name)` is nil, fall back to a GROUND START at `S.base_pos[base_name]` (land.getHeight for alt); route depart = `TakeOffGroundHot`/`From Ground Area Hot`, RTB = a Turning Point hover over the base; only sets `airdromeId` + TakeOff/Land when a real airbase exists. Log now uses `base_name` (was `home_ab:getName()` → would nil-crash). **Helis now LAUNCH from FARP-farp-4 and airbases (confirmed in log).**
- **RESOLVED — helis launch AND persist.** The "0 live" was a red herring: `Group:getSize()` returns 0 in the same frame as `addGroup` (spawn is async); after a few seconds the units are alive. Verified live: isolated Mi-24V ground start = alive (surfaceType 1/land), Mi-24V at airbase parking = alive, AH-64D ground start = alive, and a live campaign section `Heli-CAS-1-2023` (2×). The earlier count windows just happened to fall between the 15-min CAS cycles (helis RTB/die between waves). The one `TEST-MI24=gone` was a COLLISION (two helis spawned on the identical spot), not a spawn failure. Mi-24V and AH-64D both spawn/survive; type name `Mi-24V`/`AH-64D` are valid. **Heli war from FARPs is working.**
- Minor follow-up (not blocking): CAS was seen assigning to out-of-theatre bases (Beslan/Sukhumi) — check base-ownership/range scoping so the rotary war stays in the authored theatre.
- Build/check clean; fix is in the current dist.

### Ground war "slow and limited" — DIVERGENCE found + directive for the fix
- Observed: 5 BLUE + 3 RED frontline groups, "BLUE ground: vehicle reserve exhausted", veh=4 seeded, "RED survival mode (str=25) — ground strike skipped", 80 live ground units.
- Pace is C-EXACT and fine: `ground_forces.lua GND_PERIOD=12min` + `COL_SPEED=8 m/s` cite highlevl.c:250.
- DIVERGENCE (density): port model = "standing frontline, ONE group per frontline BASE" (ground_forces.lua:12). EECH `order.c:91 initialise_armoured_divisions` creates an **ARMOURED COMPANY at EVERY keysite** (order.c:361 `create_new_division(ENTITY_SUB_TYPE_DIVISION_ARMOURED_COMPANY, side, division, keysite, TRUE)`) + ARTILLERY_COMPANY per keysite (order.c:437) + airborne HC/FW/transport companies per keysite (order.c:507-525). So EECH's ground OOB scales with keysite COUNT (dozens/side); the port fields ~5/3 from a tiny finite vehicle reserve. Compounded by the mostly-empty (pending) keysites contributing nothing + weak production (1 producer, empty factories → ammo=0 → no vehicle replenishment).
- **USER DIRECTIVE (the fix): "if the site is empty default to some well-crafted templates that are used."** Empty keysite zone → spawn a well-crafted default TEMPLATE (register spawned units/statics as the keysite's assets so they still drain). This REVERSES the "pending/excluded" behavior I added earlier (empty → templated, not dead), and maps onto EECH's per-keysite company: a keysite template must include its GARRISON (armoured + artillery company) so the ground force scales with keysites.
- TEMPLATE PLAN (per keysite type; empty-zone fallback only, placed statics still override):
  - airbase/farp → garrison (armoured company + AAA/short-range SAM) + a few fuel/ammo statics
  - factory/refinery/port → production buildings + light garrison (this restores the economy → vehicle replenishment)
  - radar → EWR unit + SAM ring
  - depot/fuel/command/power → the building + defenders
  Implementation: in installations.lua/keysite.lua zone branch, when total assets == 0, spawn the type's template at the zone centre and register the spawned objects into rec.assets/rec.scenery so drain + strike + capture all keep working. Keep `pending` only for zones we deliberately can't template.
- Secondary (revisit after templates): confirm the per-side vehicle reserve seed (RESERVE_PER_BASE.vehicle=2 is a tuned proxy for EECH parser.c:1303 force_info_reserve_hardware — verify against faction data) and STRENGTH_SURVIVAL (journal-flagged 30→40) so RED doesn't go survival-defensive too early.

### Empty-site TEMPLATES — IMPLEMENTED (ground-war density fix)
- `installations.lua`: added `GARRISON` (order.c company: 1 tank + 2 IFV + 1 scout — BLUE M-1/Bradley/HMMWV, RED T-80UD/BMP-2/BTR-80) + `spawn_keysite_template(kname,kind,side,pos)` = the type building (reuse `spawn_installation`) + an armoured garrison company. In `init_from_zones`, when `ntotal==0` the site is TEMPLATED (spawned statics appended to `assets`, `templated=true`, pending/low-target cleared). Placed statics still override.
- RESULT (live, verified): keysites `16 pending → 0`; producers `1 → 8` (empty factories/refineries now produce → economy + vehicle replenishment restored); live ground units `74 → 144` via `15 garrison groups`; 0 crash; check/build clean; fix in dist.
- This is the EECH-aligned fix for the "slow and limited" ground war (per-keysite company, order.c:361) AND fulfils the user's "empty → well-crafted template" directive. Supersedes the earlier pending/excluded behavior.
- FOLLOW-UPS (next session): (1) templates currently cover NON-airbase keysites (installations.lua) — extend the same empty→template to airbase/farp zones in keysite.lua (garrison + AD ring at empty airfields). (2) Add AD to the garrison template (reuse base_defenses SA-6/Roland types) so templated sites have air defence, not just armour. (3) Register the garrison GROUP as a keysite asset too (currently only the building static drains; garrison death should feed capture/health). (4) Tune template composition by keysite type (factory=light garrison, airbase=heavy) rather than one company for all. (5) Still open from before: vehicle-reserve seed vs parser.c:1303, STRENGTH_SURVIVAL 30→40, CAS out-of-theatre base scoping.

### Templates → complex + "lift from EECH" investigation
- Upgraded `spawn_keysite_template` to a multi-building COMPLEX per type (4-5 verified KINDS statics laid out with dx/dz offsets) + garrison company + an AAA piece (Vulcan/Shilka, verified from base_defenses). Build/check clean, in dist.
- EECH-LIFT finding (user asked "can we lift templates from EECH"):
  - LIFTABLE (force structure, NOT buildings): `dv_dbase.c` — ARMOURED_COMPANY → ARMOURED_DIVISION at max 4 companies/division; ARTILLERY_COMPANY max 2/division. Per-keysite company = order.c:361. The concrete platoon vehicle composition (types+counts per group) is in EECH's `group_database` (E:/eech_source_code/aphavoc/source/gwutcfg.c or wutcfg.c) — NEXT: read it and replace the placeholder GARRISON (1 tank+2 IFV+1 scout) with the real company composition (structured as 4-company divisions + an artillery company per keysite).
  - NOT LIFTABLE (buildings): EECH keysites are TERRAIN 3D-object clusters placed by popread.c — no per-type building template exists in EECH data. The building COMPLEX is inherently a DCS-side authored proxy; keep tuning it by eye. Document this divergence (it's a faithful proxy, not a port of specific EECH data).

### Site templates → shorthand DSL + verified palette (IMPLEMENTED)
- Reworked `installations.lua` templates into a shorthand you reason about organically: `TEMPLATES[kind] = { b = {palette_token=count}, d = {garrison/aaa/sam/ewr} }`. A transpiler (`spawn_keysite_template`) flattens `b` onto a centred ~45 m grid and rings it with the `d` defence force (`spawn_palette_static` + `spawn_ground`). Reasoned per type (factory = 3 production halls + warehouses + ammo + fuel; refinery = tank farm; command = HQ + bunker + point SAM; radar = EWR + SAM battery; etc.).
- PALETTE = verified `type|shape_name|category` triples, EXTRACTED from the user's ME-placed statics via `env.mission.coalition[].country[].static.group[].units[]` (the reliable source — guessing triples/categories FAILS): factory=Boiler-house A/kotelnaya_a, warehouse=Warehouse/sklad, fueltank=Tank/bak, ammo=.Ammunition depot/SkladC, fuel=FARP Fuel Depot/GSM Rus, command=.Command Center/ComCenter, bunker=FARP CP Blindage/kp_ug, container=M92_10Ft_Container. Units from ground_forces (M-1/T-80 company) + base_defenses (Vulcan/Shilka AAA, Roland/Kub SAM, Hawk sr/55G6 EWR).
- RESULT (live): 77 statics; template buildings by kind factory:22 command:15 refinery:5 port:5 power:4 radar:4; defence groups def/sam/ewr. Build/check clean, in dist.
- KNOWN ISSUE (next): building grid + the −120 m south defence offset drop some spawns on WATER/slopes at coastal/mountain keysites (factory 5/7 at Batumi; def groups only ~4 survived). FIX: snap every template spawn XY to valid land (land.getSurfaceType == LAND, else spiral-search nearest land) before addStaticObject/addGroup — reuse for the whole template layout. NOTE: `addStaticObject` via a manual net.dostring_in probe FAILS even for known-good triples (returns an error table) — do NOT trust direct-probe results for triple validity; the campaign-init spawn path is the real test (env.mission extraction is the source of truth for triples).
- Where EECH data can still improve this: replace the placeholder GARRISON with the real `group_database` (wutcfg.c) company composition (see prior entry). Buildings stay a DCS proxy (EECH keysites are terrain).

### Template polish saga — palette, shorthand DSL, snap (reverted), stale-static reset fix
- DONE + CLEAN: shorthand `TEMPLATES[kind] = { b={token=count}, d={garrison/aaa/sam/ewr} }` + transpiler (`spawn_keysite_template` → `spawn_palette_static` grid + `spawn_ground` per-element groups). 8-type verified `PALETTE` extracted from ME `env.mission` statics (factory/warehouse/fueltank/ammo/fuel/command/bunker/container). Defence split into separate groups per element (a bad unit type only loses that element).
- SNAP EXPERIMENT — REVERTED. Added `snap_land` (nudge spawns off water) but it broke everything: the analyser flagged `cx` nil and at runtime `spawn_keysite_template` errored before spawning. Root symptom masked by STALE STATICS. Removed snap entirely → check clean again. Terrain drops on coastal keysites are back as a minor known issue (revisit with a *careful* land-snap later, or just accept a few losses).
- REAL BUG FOUND + FIXED: statics survive re-injection (reset.nuke only auto-clears GROUPS), so stale template buildings (KS-*-b*) from prior injects made empty keysites look populated → `statics_in_zone` non-empty → template + its garrison SKIPPED, while the garrison GROUPS were wiped by reset → `defence: NONE` on every re-inject. FIX: `reset.nuke` now also destroys campaign-spawned statics matching `^KS-/^FARP-/^Inst-` (author-placed keep their own names). Verified: "reset … destroyed 22 group(s), 55 campaign static(s)". This is why ALL my re-inject template measurements were polluted — the ONLY reliable test is a FRESH mission + SINGLE inject.
- STATE: build 29 modules 0 warnings, check 31 files 0 findings, in dist. DCS disconnected before the final clean verify. NEXT SESSION: fresh mission + one inject, confirm each empty keysite spawns its building complex + garrison/AAA/SAM/EWR groups (spawn_ground). If defence still sparse, it's coastal terrain (add careful land-snap) — NOT the unit types (M-1/T-80 verified; AAA/SAM/EWR from base_defenses, unverified-in-zone-mode but split into own groups so a bad one is isolated & visible).

### Template verify (DCS live again) — PARTIAL, narrowed bug
- Clean single inject (reset cleared 35 groups + 54 campaign statics). Result: defence groups `aaa:4 ewr:4 sam:4`, `def:0` (NO garrison), statics=26.
- DIAGNOSIS: only the 4 RADAR keysites templated (radar d={ewr,sam,aaa}, b={bunker=1}). statics 26 = 22 placed + 4 radar bunkers. The factory/command/refinery/power/depot/fuel keysites — though EMPTY — did NOT template (no buildings, no garrison, no aaa). spawn_queue log shows only KS-radar-* spawns; NO KS-*-def ever queued.
- The ONLY structural difference: radar's template has NO `garrison`; every other kind calls `spawn_ground(kname.."-def", side, GARRISON_UNITS[side] or {}, ...)` FIRST, and the rest of that keysite's template never runs. So the garrison spawn path is aborting spawn_keysite_template for non-radar keysites (radar skips it → works fully). GARRISON_UNITS/spawn_ground look correct on paper; addGroup is pcall'd so shouldn't throw — needs a targeted probe NEXT SESSION: add a temporary `env.info` at the TOP of spawn_keysite_template AND before/after the `-def` spawn_ground for a factory keysite to see exactly where it stops (whether spawn_keysite_template is even entered for factory, and whether the -def call returns). Suspect: GARRISON_UNITS[side] indexing (side value?) or a throw in the units-loop before the pcall.
- Shorthand DSL + 8-type PALETTE + reset campaign-static clear are all IN and CLEAN (build 29/0, check 31/0, in dist). Radar templates prove the transpiler + palette + spawn_ground all work.
- CORRECTED ROOT CAUSE (strong): NOT the garrison ordering — it's the SCENERY registration. `init_from_zones` gates the template on `ntotal == 0` where `ntotal = #statics + #scenery`. Non-radar keysite zones sit near towns and register nearby civilian `SKLADIK`/`TOPLIVO-BAK` resource scenery (via `zones.scenery_in_zone`), so `ntotal > 0` → the zone is NOT "empty" → template SKIPPED. Radar zones happen to have no such scenery → empty → templated fully. So statics=26 (22 placed + 4 radar bunkers), def:0 (radar has no garrison).
- FIX APPLIED + VERIFIED: changed the template gate in `init_from_zones` from `if ntotal == 0` to `if nstatics == 0` (placed statics only; scenery no longer blocks templating). RESULT (live): statics 26→**84**, ground units 78→**134**, garrison groups (def) 0→**11**, aaa **12**, sam **4**, ewr **1**; building complexes factory:35 command:9 refinery:6 port:6 power:5 radar:1. ALL keysite types now template full complexes + garrison + AD. Check 31/0, build 29/0, in dist. **Site templates COMPLETE** — shorthand DSL + verified palette + per-type complexes + per-keysite garrison, EECH-aligned ground density. Remaining polish (low priority): coastal terrain can still drop a few spawns (careful land-snap later); replace placeholder GARRISON with real group_database composition.

### 8x scenario watch — full mechanics audit
- WORKING (verified live at 8x): OCA strikes/sweeps, reaction/CAP scrambles, HELI WAR (CAS sections from FARPs — the fix holds), keysite strikes (installations drain to 0 → "DESTROYED, capturable"), ground columns advancing (GndCol base→base), troop insertion (fires in mid phase), economy/production (crates ammo/fuel, growing reserves), strength self-corrects to ~50/50, phase progression early→mid→late, templates/garrisons.
- STALE-SCHEDULER CONFIRMED + its impact: re-injecting many times left OLD generations running (STATUS interleaved T+76m AND T+8m = two concurrent campaigns). This DOUBLED task demand → task failures 35. A FRESH restart + SINGLE inject → monotonic STATUS + failures dropped to **2**. So the generation guard is NOT fully cancelling old loops across re-inject; the ONLY clean test is fresh mission + one inject. (Backlog: make the guard actually kill old schedulers, or accept restart-per-test.)
- CAPTURES NEVER COMPLETE — root-caused: `keysite.try_capture` flips a base when efficiency < MINIMUM_EFFICIENCY(0.3) AND an enemy ground group is within CAPTURE_RANGE(5000m). Mechanism is correct. But BASES never reach <0.3: watched them dip to 66-72% then REPAIR back to 100%. Cause = `keysite_repair.lua REPAIR_RATE=0.05/min` UNCONDITIONAL auto-heal vs `attack_waves KEYSITE_STRIKE_DMG=0.28`. The repair comment self-admits it's a PROXY: "EECH repair requires a dedicated repair unit task; we proxy this as automatic health increment" — so port bases are far more resilient than EECH → focused strikes can't hold a base under 0.3 → no base captures → win condition can't trigger → permanent balanced stalemate (BLUE~50 RED~50).
- FIX (prime-directive, NEXT): trace EECH `ks_updt.c` update_server repair sub-loop and realign `keysite_repair.lua` — repair should be CONDITIONAL (repair-task + supply gated) and/or slower, not unconditional 5%/min. Then bases can be neutralised → captured → front advances → campaign resolves. This is the single change between "all mechanics fire" and "campaign plays to a conclusion".
- Minor: installation keysites (port/factory) DO drain to capturable but try_capture only scans S.base_owner (bases) — objectives are bases so OK, but note the asymmetry.

### REPAIR aligned to EECH (DONE) → exposed the capture chain's real blocker
- FIX APPLIED: `keysite_repair.lua REPAIR_RATE_PER_TICK` 1/20 → **1/100** (0.05→0.01/min), realigned to EECH's ACTUAL repair (traced live): `ks_updt.c` update_server only repairs in KEYSITE_STATE_REPAIRING (after `create_repair_task`); `keysite.c:1581 repair_client_server_entity_keysite` restores ONE building then `repair_timer = 10.0*ONE_MINUTE` (regen buildings 20 min). So a multi-building airbase takes ~10min×N (50-100+ min) to fully repair — NOT the 20 min the old proxy assumed. The 8-min STRIKE_SUPPRESS gate + REPAIR_MIN_SUPPLY 50% were already present; only the rate was 5× too fast.
- VERIFIED live at 8x: bases now genuinely wear down and NEUTRALISE — FARP-farp-3 went 72%→57%→10%→**0%** (before the fix they floored at 66% and recovered to 100%). Build/check clean, in dist. **The prime-directive repair divergence is closed.**
- CAPTURE CHAIN — now exposed the REAL blocker (one link past repair): strike→neutralise ✅ → `T.I.: FARP-farp-3 capturable (0%) — tasking insertion` ✅ (troop insertion IS scheduled every 2min and DOES target the neutralised base) → ❌ `task_board: troop_insertion vs FARP-farp-3 EXPIRED after 48min unassigned → FAILED (no suitable idle group)`, 0 insert helis spawn. ROOT: `FARP-farp-3 airbase exists=false` — synthetic zone-FARPs have NO HELIPAD, so the transport/insert heli can't be assigned/spawned to (nor land at) a padless base. SAME root as the earlier CAS build_attack_heli fix.
- NEXT (closes the campaign loop): give the troop-insertion transport-heli path the SAME padless-FARP treatment as `heli_war.build_attack_heli` — spawn from base coordinates (ground start) when no Airbase, and detect the insertion "landing" by PROXIMITY to the target base's coords (not an airbase-Land waypoint) so the capture_roll fires. Then: strike→neutralise→insert→capture→front advances→win resolves. Also apply the padless-FARP ground-start to any other heli spawner that still assumes `Airbase.getByName(base)`.

### CAPTURE CHAIN CLOSED — campaign loop now resolves end-to-end
- FIX 2 APPLIED: `troop.lua build_troop_insert` — same padless-FARP ground-start as heli_war.build_attack_heli. When `Airbase.getByName(base_name)==nil`, spawn from `cs.S.base_pos[base_name]` with a `TakeOffGroundHot`/`From Ground Area Hot` depart (no airdromeId), else the normal TakeOff-from-parking. Capture already triggers on DISPATCH+CAPTURE_DELAY (proximity/stand-off model), so only the SPAWN needed fixing.
- VERIFIED LIVE (8x, clean run): base wears to 26% → `T.I.: FARP-farp-4 capturable — tasking insertion` → `BLUE T.I. #2030 → FARP-farp-4 FROM FARP-farp-3` (dispatched from a PADLESS FARP — ti-fail now 0, was "no suitable idle group") → `CAPTURE: BLUE seizes FARP-farp-4 from RED (health→90%, +6 heli/+4 fw reseed)` + `T.I. CAPTURE: FARP-farp-4 → BLUE` + `regen: capture reseed`. FARP-farp-4 flipped RED→BLUE and held. Build/check clean, in dist.
- **FULL LOOP CONFIRMED**: strike → (EECH-aligned repair lets it) neutralise → troop insertion → capture → instant-repair + regen reseed → front advances → win re-check. The permanent stalemate is gone; the campaign can now play to a conclusion. Both blockers were EECH-alignment fixes (repair rate; padless-FARP heli spawn), not new invented logic.
- OUTSTANDING (lower priority now the loop works): stale-scheduler cancellation across re-injects (fresh restart per clean test); the earlier template terrain-snap + real group_database garrison composition; verify win actually FIRES once a side captures all its objective keysites.

### On-capture repair realigned to EECH (was another tuned divergence) — front now DYNAMIC
- User caught it: captured bases snapped to a flat 90% (`CAPTURE_REPAIR_HEALTH=0.9`). EECH `keysite.c` capture_keysite repairs UP TO 5 buildings (for loop<5 repair_client_server_entity_keysite) then USABLE-if-full else REPAIRING — an ADD, not a jump. The port's own comment still described the faithful 0.5; it had been tuned to 0.9 to stop instant re-capture.
- FIX: `keysite.do_capture` now `base_health = min(1.0, current + CAPTURE_REPAIR_BUILDINGS)` where `CAPTURE_REPAIR_BUILDINGS=0.5` (5 building-equivalents × 0.1, consistent with the 0.01/min repair rate), then slow keysite_repair finishes it. Build/check clean, in dist.
- VERIFIED LIVE (8x): captures now come back at **60% / 72% / 71%** (current+0.5), NOT 90%. BONUS — the front is now DYNAMIC: FARP-farp-3 changed hands 3× (RED↔BLUE) because a ~65% base is re-takeable, and strength broke from 50/50 to **BLUE=36 RED=64** (RED pulling ahead → campaign progressing toward a WINNER, no longer a frozen stalemate). The old flat 0.9 was freezing the front.
- FULL EECH-ALIGNED LOOP CONFIRMED end to end: strike → neutralise(10%) → troop insertion (padless-FARP ground start) → capture → repair 5 buildings(~65%) → contest → front shifts → strength diverges. Three EECH-alignment fixes this session (repair rate, padless-FARP heli spawn ×2, on-capture repair) turned a permanent stalemate into a resolving campaign — all traced to EECH source, no invented logic. LAST CHECK: confirm the win actually fires when a side takes all objective keysites.

### On-capture repair made SIZE-AWARE (closer to EECH) — front no longer oscillates
- Problem after the +0.5 fix: FARPs came back at flat ~65% and instantly flip-flopped (front oscillated on ONE base, never advanced → no win). Root: EECH's "repair 5 buildings" is SIZE-dependent — 5 buildings ≈ ALL of a small FARP (→ full → held) but only PARTIAL of a large airbase (→ contestable). The flat +0.5 ignored size.
- FIX: `keysite.do_capture` bump is now `base_kind`-aware — `CAPTURE_REPAIR_SMALL=1.0` (FARP/FOB → full, held) vs `CAPTURE_REPAIR_LARGE=0.4` (airbase → partial, contestable), mirroring EECH air_force_capacity SMALL vs LARGE. Build/check clean, in dist.
- VERIFIED LIVE: `CAPTURE: RED seizes FARP-farp-3 from BLUE (health→100%)` — captured FARP now returns at 100% and RED HOLDS it (farp-3/4/farp all RED 100%, no flip-flop). Front consolidated; RED ahead 52/48, positioned to grind BLUE's rear airbases (partial-repair, contestable) toward the objective win.
- Full end-to-end EECH-aligned campaign now: strike→neutralise→troop-insertion(padless-FARP)→capture→size-aware repair (FARP held / airbase contestable)→front ADVANCES. FOUR alignment fixes this session (repair rate 0.05→0.01; padless-FARP heli spawn for CAS+troop; on-capture repair flat-0.9→+5-buildings→size-aware) — every one traced to EECH source. Remaining: watch a full run to a DECLARED win (objectives / no-usable-airbase / no-combat-heli) now that the front advances.

### Watched to a dominant-but-undeclared state — next blocker is STRIKE TARGETING of rear objectives
- Size-aware capture repair CONFIRMED holding: FARP-farp-3 captured → 100% and HELD (no oscillation). Campaign dynamic, RED dominant (61 vs 39 at T+98m).
- BUT win never fired: captures=1 (only the forward FARP), and BLUE's REAR objective keysites stay at 100% — Batumi=BLUE(100%), FARP-farp-2=BLUE(100%), Kutaisi untouched. They are NEVER neutralised → never capturable → RED can't take them → the "all objectives captured" / "no usable airbase" win can't trigger despite RED clearly winning on strength.
- NEXT DISTINCT BLOCKER (separate from repair, now the #1 item to close a declared win): strikes hammer only the immediate contested line (farp-1 ~55%, farp-3) and never ROTATE onto / REACH the deep objective keysites (Batumi/Kutaisi/farp-2 at 100%). Investigate: (a) keysite/OCA strike TARGET SELECTION — is it distance-weighted so the deep rear is never top-N? (attack_waves KEYSITE_STRIKE_DMG / installations.strike_targets_ranked proximity bias). (b) MAX_ASSIGN_RANGE / reachability — are the rear objectives simply out of striker range from the front? (c) does the tasker prioritise objective keysites at all, or just nearest enemy keysites? Trace EECH highlevl.c create_keysite_strike_tasks target scoring (imap IMPORTANCE incl. objective/campaign value) and align. Only then does a dominant side actually reduce the enemy to no-usable-airbase and WIN.
- SESSION TALLY (all EECH-aligned, no invented tuning): repair rate 0.05→0.01; padless-FARP ground-start for CAS + troop-insertion helis; on-capture repair flat-0.9 → +5-buildings → size-aware (FARP full / airbase partial). Campaign went from frozen 50/50 stalemate → dynamic, resolving, one side dominant. Last mile to a DECLARED win = strike targeting of the deep objectives.

### Strike targeting aligned to EECH imap (importance × reachability) — front now ADVANCES
- Root of the "dominant side can't win" stall: `keysite.pick_targets` FORCE-slotted objectives first regardless of reachability → tasked out-of-range deep objectives whose strikes just EXPIRED unassigned → front froze while objectives sat at 100%. (The 4-layer imap in imap.lua was built like EECH's but had NO consumer — dead code; rate_pos already had a proximity/reachability term though.)
- FIX (EECH-faithful): objectives are no longer force-slotted. `keysite.rate_pos` now adds an objective IMPORTANCE boost SCALED by reachability — `obj_boost = is_objective and 4.0*proximity` (EECH imap product importance × base-distance). `rate_target` passes is_objective (checks S.objectives[attacker]); `pick_targets` just sorts by rating and takes top-N. So a REACHABLE objective outranks everything, a deep out-of-range one does not → top-N are reachable high-value targets → front rolls forward, bringing the next objective into range. Build/check clean, in dist.
- VERIFIED LIVE: `target: BLUE → FARP-farp-4 (rating=9.46)` (reachable objective correctly top-rated) → `CAPTURE: BLUE seizes FARP-farp-4 (health→100%)` held → BLUE dominant 71/29, Batumi being worn to 94% (front advancing toward the NEXT objectives) — the exact opposite of the pre-fix frozen oscillation. The front now advances toward the objective ring.
- NOT yet witnessed: the final VICTORY declaration — a fresh `Campaign live` re-boot fired mid-watch (stale-scheduler / re-injection pollution) and reset the run before it completed. So the mechanism is proven (front advances to objectives) but a CLEAN uninterrupted run to completion is still needed to see the win banner. → the stale-scheduler cancellation (make _DMT_GEN actually kill old loops / avoid spurious re-boots) is now the #1 blocker to witnessing a full playthrough, and is the last thing standing between "all mechanics fire + front advances" and "watched it declare a winner".
- SESSION: FIVE EECH-alignment fixes total (repair rate; padless-FARP heli spawn ×2; on-capture repair size-aware; strike targeting importance×reachability) — turned a frozen 50/50 stalemate into a campaign with a moving front that concentrates on and captures objectives. Every fix traced to EECH source.

### Stale-scheduler investigation: guard WORKS; clean run exposes strike-CONCENTRATION as the real win-blocker
- Investigated the generation guard properly: `_DMT_GEN=9` live (increments every inject), EVERY "Campaign live" boot pairs 1:1 with a `reset.nuke` at the same timestamp (all legitimate re-loads, none stale), no `_G.game_loop`/`_G.dynamic_mission_test` leak. CONCLUSION: the guard functions; the "interleaved STATUS / stale schedulers" earlier was largely LOG-HISTORY misread across my ~9 re-injects this session (each fix re-injected → reset → no single run ever completed). All RECURRING schedulers are guarded; the only unguarded ones were reaction.lua's 2 ONE-SHOT cleanup timers (cap-expiry, BDA-loiter).
- FIX: added `local my_gen=_DMT_GEN` capture + `if _DMT_GEN~=my_gen then return nil end` to both reaction.lua one-shot timers (prevents an old-gen timer touching new-gen groups/reserves post-reinject). Build/check clean, in dist. gen=10.
- CLEAN SINGLE-GEN RUN (injected once, did NOT re-inject — boots stayed 1 the whole run): confirms the campaign runs cleanly and PROGRESSES — RED captured FARP-farp-3 and HELD it (size-aware repair), BLUE collapsed to 32 (survival, 12 skips), lowest base 14%. So all five fixes work together on a clean run.
- BUT the win still does NOT declare — and the clean run reveals WHY, distinct from everything fixed so far: RED takes the forward FARPs but BLUE's REAR objectives (Batumi/Kutaisi/FARP-farp-1/2) stay pinned at 100%. On ONE generation's strike volume (no double-gen inflating it) RED's strikes SPREAD across bases and each rear base REPAIRS back to 100% between hits → nothing crosses the 30% capture threshold → no objective captures → no win. (The earlier "captures work" demos were partly inflated by double-generation 2× strike volume.)
- CONCRETE EECH-DIVERGENCE LEAD (next fix, do NOT feel-tune — trace it): `keysite.rate_pos` scoring includes `2*damage` — this makes ALREADY-DAMAGED bases rate higher, pulling strikes onto the contested front instead of CONCENTRATING on a fresh objective until it falls. EECH's IMAP_IMPORTANCE is strategic keysite value, NOT current damage — so the damage term De-CONCENTRATES effort, opposite of EECH's "all arms pile onto the objective." Likely fix: drop/greatly reduce the damage term so a reachable objective stays consistently top-rated and gets struck repeatedly → neutralised → captured → next. Also re-examine keysite strike COUNT/cadence per cycle vs EECH create_keysite_strike_tasks (concentration vs spread). THIS (strike concentration vs repair on a single generation) is now the true last blocker to a DECLARED win — the stale-scheduler concern is resolved.

### BLUE fielded no Apaches — root cause = unloaded AH-64D module (NOT a code bug); built spawn integration test
- Symptom (user spotted): live campaign BLUE helis=0 vs RED=4 despite BLUE heli reserve=27. BLUE fielded only Insert-/Regen- (transport) helis, never attack/CAS AH-64D.
- ROOT CAUSE (DCS engine log, reliable): `Error: Unit [AH-64D]: Corrupt damage model` + `Can't open file: /textures/liveries/ah-64d_blk_ii/...` — the AH-64D (Apache) module's assets weren't loaded in this install, so every script-spawned Apache silently fails to create (addGroup returns ok, no unit appears; build_attack_heli logs only AFTER a successful spawn → 0 logged attempts). RED Mi-24V unaffected. SA342M/UH-1H also showed corrupt at first.
- FIX (user, mission-side): PLACE ONE AH-64D in the .miz. That forces DCS to fully load the module's damage model + textures, after which ALL script-spawned Apaches work. This is a MISSION-AUTHORING requirement, not a code change — AH-64D stays as BLUE's faithful attack heli (EECH US side = Apache/Comanche); swapping the type would be an unfaithful workaround. → worth a README note: the mission must place one of each MODULE aircraft the campaign uses (AH-64D) to preload assets.
- NEW TOOL (user-requested): `Scripts/dynamic-mission-test/test/spawn_test.lua` — integration test that spawns EVERY campaign unit type + payload in isolation and reports PASS/FAIL. Key design points learned the hard way: (1) groups are NOT queryable in the frame they're created via net.dostring_in → must spawn then VERIFY on a deferred timer (+4s); (2) ground/static probes must be nudged to LAND (coastal origin like Batumi drops grid cells in the sea) AND kept ≥600 m apart (else land-nudging collapses clustered probes onto one strip → collisions → false FAILs). Attack helis are tested BOTH bare and payload-loaded to isolate airframe vs CLSID.
- RESULT: 30/30 PASS once module loaded — AH-64D (bare + AH64D_CAP), Mi-24V (bare + MI24V_CAP), all fixed-wing, all transport helis, all 14 ground units (garrison/AAA/SAM/EWR/SHORAD), all 4 statics. CONCLUSION: no bad type names, no wrong CLSIDs anywhere in the campaign; the only issue was the unloaded Apache module. Run anytime via dofile of the test; results under log tag [spawn_test].

### BLUE fielded no Apaches IN THE CAMPAIGN — root cause = offset=0 timer bug (found via instrumentation)
- After the module-load fix, the spawn integration test showed AH-64D spawns fine, yet the CAMPAIGN still fielded 0 BLUE attack helis (only Insert-/Regen-). User: "add a lot more logging." Did so — [cas-dbg] (CAS target funnel + scheduler lifecycle), [board-dbg] (one-shot base-assignment funnel: owned/in_range/stocked/free_slot), [heli-dbg] (build_attack_heli entry + failure exits).
- The logs walked it right to the bug: `BLUE schedule_cas REGISTERED (offset=0)` but `BLUE CAS timer FIRE` NEVER appeared (RED's did). ROOT CAUSE: `game_loop.lua:248 cas_m.schedule_cas(BLUE, 0, ...)` — the ONLY scheduler in the whole game loop with offset 0. `timer.scheduleFunction(fn, nil, timer.getTime()+0)` schedules at the injection instant, and DCS SILENTLY DROPS a timer scheduled at/before "now" → BLUE's CAS scheduler never ran → 0 CAS heli tasks → 0 Apaches. Nothing to do with the module, payload CLSIDs, ledger, or fidelity. (Also surfaced but SEPARATE: striker/recon show stocked=0 on both sides when their stocked airbases are lost — a different thread.)
- FIX: `game_loop.lua:248` offset 0 → 5 (BLUE CAS fires 5 s in, still staggered before RED's 30 s). HARDENED: `schedule_cas` now clamps `offset = math.max(initial_offset or OFFSET_CAS, 1)` so a 0 can never silently disable a side again. VERIFIED live: `BLUE cas heli section (2xAH-64D) from FARP-farp-3` — BLUE launches 2 Apache sections (≥ RED). Build/check clean.
- Kept the useful operational logging ([cas-dbg] target funnel, [board-dbg] one-shot assignment funnel, [heli-dbg] spawn failures); removed the pure bug-hunt traces (timer FIRE / run_cas ENTER / build ENTER / REGISTERED).
- FOLLOW-UP: the same offset>0 guard should ideally be applied to the other schedule_* fns defensively (all are currently CALLED with positive offsets, so none is broken today, but the class of bug is latent). Also: `Scripts/dynamic-mission-test/test/spawn_test.lua` remains a reusable spawn integration test (30/30 once the AH-64D module is loaded via a placed unit).

### Codebase-wide structured logging added
- Infra: `campaign_state.M.dbg(tag, fmt, ...)` → `env.info("[dmt:<tag>] ...")`, gated by global `_DMT_DEBUG` (default ON; `net.dostring_in('server','_DMT_DEBUG=false')` to silence), pcall-guarded so a bad format never aborts a tick.
- ~232 `cs.dbg` calls added across 27 modules (payloads.lua skipped) via one agent, at decision/flow points: scheduler register+fire (with offset — so the offset=0 class of bug is now glaringly visible), target-selection funnels (N candidates→M filtered→K tasks), task_board assignment funnel (owned/in_range/stocked/free_slot), supply ledger consume/recycle before→after (the "stocked=0" blind spot), captures/damage/efficiency, repair ticks, win-criterion telemetry, reaction chain triggers, regen queue/spawn, etc. The 4 old ad-hoc `[cas-dbg]/[board-dbg]/[heli-dbg]` strings migrated to `cs.dbg`.
- game_loop dumps a STARTUP SCHEDULER ROSTER (`scheduler <name>/<side> offset=Xs`) — makes an offset bug obvious at a glance.
- VERIFIED: check clean (0 findings), build 0 warnings, injected — 20 tags streaming (436 lines/90s), 0 FMT-ERR, BLUE still flying Apaches.
- LATENT BUGS the agent flagged while reading (NOT fixed — future work): (1) `keysite.base_slot_capacity` always returns LARGE(8) even for FARPs/FOBs — `SLOT_CAPACITY.SMALL=4` is defined but unused, so forward FARPs get airbase-sized landing slots. (2) `regen.queue_insert` always returns true (evicts oldest on overflow), so the "queue full → dropped" else-branch in make_dead_handler is dead code masking a silent oldest-regen drop. (3) main.lua double-start footgun (CLAUDE.md P2) now has dbg at both call sites to surface it if it double-fires. (4) heli_war's retired schedule_anti_armour/hunter_killer don't clamp offset>0 like schedule_cas now does — latent re-occurrence of the silent-timer-drop bug if ever re-enabled.

### Fixed the 4 latent bugs surfaced by the logging pass
1. `keysite.base_slot_capacity` — was hardcoded LARGE(8) for every base; now size-aware via base_kind: FARP/FOB → SMALL(4), airbase → LARGE(8) (mirrors EECH air_force_capacity SMALL/LARGE, same split do_capture uses). Forward FARPs no longer get airbase-sized landing-slot capacity → tighter slot throttle on small bases.
2. `regen.queue_insert` — ring-buffer overflow (evict oldest, EECH rg_updt.c:791-794) was silent and the make_dead_handler "queue full" else-branch was dead. queue_insert now returns (true, evicted); the dead branch became a reachable OVERFLOW log so a saturated regen queue (lost tempo) is visible. Behaviour unchanged (still overwrites oldest) — just observable.
3. `main.lua` double-start footgun (CLAUDE.md P2) — removed the legacy `_G.game_loop` handoff branch entirely; `game_loop.start()` now runs EXACTLY ONCE via the unconditional `require("game_loop").start()` at file end. VERIFIED live: 1 "game loop starting" per inject, handoff-branch-fired=0.
4. `heli_war.schedule_anti_armour`/`schedule_hunter_killer` — offset now clamped `math.max(initial_offset or 1, 1)` (both were unclamped); retired today but this closes the exact silent-timer-drop class that killed BLUE's CAS. Stale "NOT clamped" comment updated.
- VERIFIED: check clean (0 findings), build 0 warnings, injected — single-start, BLUE flying 4 Apache sections, 0 FMT-ERR / 0 campaign errors.

## Next steps
- (If desired) re-apply the reverted reset.nuke author-placed-group preservation — one-liner, captured above — if AD survival across re-injects becomes wanted.
- Fix the stale-scheduler CLOBBERING properly (old-gen overlay/reset timers not fully cancelled across re-injects) so multi-inject doesn't wipe labels — currently worked around by fresh restart.
- Still-owed C-source fidelity cleanup (from v0.1.0 CHANGELOG): snowball scaling, STRENGTH_SURVIVAL, capture-on-dispatch, invented "military collapse" win condition, MAX_ASSIGN_RANGE, CAPTURE_REPAIR_HEALTH — trace to EECH source per prime directive.
- Verify BLUE=100 RED=0 early-boot strength is only a transient live-unit census (RED fields forces after a few min) and not a real seeding bug.
- Optional: toggle to hide the per-destructible ▪ layer (map is busy at big airfields — Kutaisi 76).

## Open questions
- Are the ~62 "warehouses" at Kutaisi all intended as keysite assets, or should a size cap apply? (User accepted them as-is this session.)

## Follow-ups & improvements
- Consider an author-drawn TIGHT resource sub-zone convention if per-airfield asset counts ever need trimming.
- `main.lua` legacy parallel layer + dead `_G.game_loop` handoff still pending retirement (backlog P2).
