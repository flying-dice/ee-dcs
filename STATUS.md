# Campaign TypeScript migration

- Goal / health: All 34 campaign Lua modules are ported to strict TypeScript-to-Lua. Implementation and offline verification complete; live acceptance remains blocked.
- Now: Root `dist/ee-dcs.lua` builds successfully. Generated FARPs use DCS's stock four-position `FARP`/`FARPS` heliport, and every helicopter spawn path assigns positions 1 through 4 through the DCS heliport link fields. The web generator uses explicit administrative-territory assignments: a district modal sets BLU/RED ownership and Rear/Close role, with no automatic role detection. Airbases inherit territorial ownership, mains must match their side, FARPs/radar use Close territory, and factories/refineries/ports/power/command/depot/fuel use Rear territory. All ten zone types recognized by the campaign runtime are generated. FARPs are generated directly inside Close administrative polygons without OSM land-use masks. Generated `farp_*` zones now materialise physical DCS FARP heliport statics at runtime under the correct CJTF country; the prior authored-zone path incorrectly assumed those objects were already in the mission. The task board retains its three-minute EECH cadence after an initial 45-second assignment pass, allowing the +5/+30-second CAS generators to launch the first helicopter sorties promptly. Generated missions and runtime spawns consistently use Combined Joint Task Forces Blue and Red. The mission F10 map now renders a coalition-scoped operational picture with objectives, a topology-derived FLOT, queued and active tasks, complete ground echelons and friendly logistics; enemy contacts are filtered through the campaign fog-of-war threshold. Coalition radio reports expose situation, air tasking, logistics/regeneration and intelligence updates. The invented elapsed-time early/mid/late labels have been removed; campaign evolution is driven by the EECH-derived threat, fog, tasking, logistics, reaction, capture and victory systems. A three-pass enum audit also fixed scenery discovery compiling to a nonexistent runtime enum, neutral units entering RED regeneration, RED client slots falling back to BLU aircraft, and persisted neutral/invalid combat ownership. Gameplay literals now use named constants with DCS, EECH, mission-format, or adapter-policy provenance documented in ADR 0003. Ported event handlers safely ignore static-object BIRTH/LAND/DEAD events instead of calling unit-only methods. Selectable territory is clipped to the DCS airfield bounding box plus a 100 km buffer, removing unused map edges. Caucasus OSM data is exported locally from `E:\planet-latest.osm.pbf` with Docker/Osmium and includes 832 simplified administrative level 6 boundaries. Deployed browsers make no Overpass requests. Mission parity tests, authored-FARP regression, roster regression, web type-check, production build and live browser assignment checks pass.
- Next: Generate a fresh `.miz` and validate the coalition-scoped F10 operational picture plus four-position FARP parking live in DCS: confirm the rendered FLOT, queued/active mission labels, fog-of-war contact changes, radio reports, physical FARPs and initial helicopter sorties.
- Later: Remove original Lua only after successful live validation. Originals remain unchanged as the comparison baseline.
- Blocker: DCS is running, but DCS Studio tools are not registered in this session and the configured `127.0.0.1:25570/mcp` endpoint is not listening. Installed DCS data, generated Lua and offline mocks do not establish live DCS acceptance. The mission-only F10 overlay and coalition reports are implemented and pass offline verification.
- Working state: 2026-09-20, branch `main`, baseline `232b9f5`. No commits or pushes. Existing untracked agent configuration is preserved.

See [sprint](docs/bots/sprints/2026-09-20-sprint-01.md).
